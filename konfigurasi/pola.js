export { jsx, js, html, css, json }

const jsx = {
    tabWidth: 3,
    useTabs: false,
    semi: false,
    jsxSingleQuote: true,
    trailingComma: 'none',
    bracketSpacing: true,
    bracketSameLine: false,
    arrowParens: 'always',
    parser: 'meriyah'
}

const js = {
    tabWidth: 3,
    useTabs: false,
    semi: false,
    singleQuote: true,
    jsxSingleQuote: true,
    trailingComma: 'none',
    bracketSpacing: true,
    bracketSameLine: false,
    arrowParens: 'always',
    parser: 'meriyah'
}

const css = {
    tabWidth: 3,
    singleQuote: true,
    parser: 'css'
}

const html = {
    tabWidth: 3,
    singleQuote: true,
    parser: 'html'
}

const json = {
    tabWidth: 3,
    parser: 'json'
}

